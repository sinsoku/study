<%= form_for(user_organization) do |f| %>
  <% if user_organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_organization.errors.count, "error") %> prohibited this user_organization from being saved:</h2>

      <ul>
      <% user_organization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :organization_id %>
    <%= f.text_field :organization_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
